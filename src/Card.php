<?php
  class Card
  {
    private $score;
    private $description;


  }
?>
card class
->score
->desc

$cardOne = new Card(rand(1,10), firstcard);
$cardOne->save();

$allCards = array($cardOne....)

$allCards[0];

draw()
if (card.getDesc == firstcard) {
  card.drawNewCard()
}
